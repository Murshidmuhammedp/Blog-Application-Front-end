/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['cdn-icons-png.flaticon.com', 'currentaffairs.adda247.com', 'www.citypng.com',"via.placeholder.com"], 
    },
};

export default nextConfig;